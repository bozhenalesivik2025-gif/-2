#include <iostream>
#include <iomanip>
#include <cmath>

using namespace std;

int main()
{
    double P1, P2;
    int i, j;

    cout << " while:" << endl;
    P1 = 1.0;
    i = 1;
    while (i <= 3) {
        P2 = 1.0;
        j = i;
        while (j <= (4 - i)) {
            P2 *= sqrt(abs(tan(i))) / (double)(i + j * j);
            j++;
        }
        P1 *= P2;
        i++;
    }
    cout << P1 << endl << endl;

    cout << "Результат (do-while):" << endl;
    P1 = 1.0;
    i = 1;
    do {
        P2 = 1.0;
        j = i;
        if (j <= (4 - i)) {
            do {
                P2 *= sqrt(abs(tan(i))) / (double)(i + j * j);
                j++;
            } while (j <= (4 - i));
        }
        P1 *= P2;
        i++;
    } while (i <= 3);
    cout << P1 << endl << endl;

    cout << "for:" << endl;
    P1 = 1.0;
    for (i = 1; i <= 3; i++) {
        P2 = 1.0;
        for (j = i; j <= (4 - i); j++) {
            P2 *= sqrt(abs(tan(i))) / (double)(i + j * j);
        }
        P1 *= P2;
    }
    cout << P1 << endl << endl;

    cout << "for:" << endl;
    P1 = 1.0;
    for (i = 3; i >= 1; i--) {
        P2 = 1.0;
        for (j = (4 - i); j >= i; j--) {
            P2 *= sqrt(abs(tan(i))) / (double)(i + j * j);
        }
        P1 *= P2;
